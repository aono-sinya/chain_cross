source "https://rubygems.org"
ruby "4.0.5"

gem "rails", "~> 8.1.3"
gem "puma", "~> 8.0"
gem "bootsnap", require: false

# Views (minimal asset pipeline)
gem "propshaft", "~> 1.3"

# Firestore (admin DB)
gem "google-cloud-firestore", "~> 3.2"

# BigQuery (transaction log sink)
gem "google-cloud-bigquery", "~> 1.64"

# Ethereum / Besu
gem "eth", "~> 0.5.17"

# HTTP for game-mock integration
gem "faraday", "~> 2.14"

# Background processing (simple in-process loop for local)
gem "concurrent-ruby", "~> 1.3"

group :development do
  gem "web-console"
end


